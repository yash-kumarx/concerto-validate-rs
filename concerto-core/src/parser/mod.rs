//! Parser modules.
//!
//! Right now "parser" mostly means "JSON loader". The CTO parser is still here
//! as a placeholder because the real runtime will need it later.

pub(crate) mod cto_parser;
pub(crate) mod json_loader;
